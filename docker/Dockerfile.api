FROM node:20-alpine
WORKDIR /workspace
COPY package.json package-lock.json* ./
RUN npm install --legacy-peer-deps || npm install --no-package-lock
COPY . .
EXPOSE 3333
CMD ["npm", "run", "start:api"]
